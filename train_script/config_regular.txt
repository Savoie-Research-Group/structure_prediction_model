spec_train <the dir where train.csv is stored>
spec_val <the dir where val.csv is stored>
loss_path <the path to save the log of loss values during the training>
mode_lst 1,0,0 #training input: the first digit represents MS, the second represents IR, the third represents H-NMR. So 1,0,0 means only use the MS, 1,1,1 means use all of the inputs
best_chk_path <the path to save the checkpoint of the lowest validation loss>
end_chk_path <the path to save the ending checkpoint>
